<li>
	{{burger_name}}

	<button class="change-devoured" data-id="{{id}}" data-newdevoured="{{devoured}}">
		{{#if devoured}}Devour!{{else}}Undevour? Gross.{{/if}}
	</button>

	<button class="delete-burger" data-id="{{this.id}}">Throw Away</button>
</li>
